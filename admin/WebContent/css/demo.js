$(document).ready(function(){
		
	// first example
	$("#navigation").treeview({
		collapsed: true,
		unique: true,
		persist: "location"
	});
	$("#navigation1").treeview({
		collapsed: true,
		unique: true,
		persist: "location"
	});
	
	
	$("#navigation2").treeview({
		collapsed: true,
		unique: true,
		persist: "location"
	});
	
	
	$("#navigation3").treeview({
		collapsed: true,
		unique: true,
		persist: "location"
	});
	
	$("#navigation4").treeview({
		collapsed: true,
		unique: true,
		persist: "location"
	});
	
	$("#navigation5").treeview({
		collapsed: true,
		unique: true,
		persist: "location"
	});
	
	$("#navigation6").treeview({
		collapsed: true,
		unique: true,
		persist: "location"
	});
	
	$("#navigation7").treeview({
		collapsed: true,
		unique: true,
		persist: "location"
	});
	
	$("#navigation8").treeview({
		collapsed: true,
		unique: true,
		persist: "location"
	});

});

